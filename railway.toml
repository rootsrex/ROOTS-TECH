[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install --include=dev && sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/' prisma/schema.prisma && npx prisma generate && npx tsc && cd ../frontend && npm install --include=dev && npm run build && cd ../backend && npx prisma db push"

[deploy]
startCommand = "cd backend && node dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "dancepay"
